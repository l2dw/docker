http:
  middlewares:
    forward-https-headers:
      headers:
        customRequestHeaders:
          X-Forwarded-Proto: "https"
          X-Forwarded-Port: "443"
    redirect-to-https:
      redirectScheme:
        scheme: https
        permanent: true
