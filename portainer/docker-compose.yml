networks:
  default:
    name: ${DEFAULT_NETWORK_NAME:-portainer-network}
    external: ${DEFAULT_NETWORK_EXTERNAL:-false}

volumes:
  portainer_data:
    name: ${PORTAINER_DATA_VOLUME_NAME:-portainer_data}
    external: ${PORTAINER_DATA_VOLUME_EXTERNAL:-false}

services:
  server:
    image: ${PORTAINER_IMAGE:-docker.io/portainer/portainer-ce:lts}
    restart: ${PORTAINER_RESTART:-unless-stopped}
    privileged: ${PORTAINER_PRIVILEGED:-false}
    command:
      - -H
      - unix:///var/run/docker.sock
      - --http-enabled
      - --base-url=${PORTAINER_BASE_PATH:-/}
      - --trusted-origins=${PORTAINER_TRUSTED_ORIGINS:-localhost,127.0.0.1,portainer.example.com}
    env_file:
      - ${PORTAINER_ENV_FILE:-.env.example}
    environment:
      - TZ=${TZ:-America/Toronto}
    volumes:
      - ${DOCKER_RUNTIME_SOCKET:-/var/run/docker.sock}:/var/run/docker.sock
      - ${PORTAINER_DATA_DIR:-portainer_data}:/data
    deploy:
      mode: ${PORTAINER_DEPLOY_MODE:-replicated}
      replicas: ${PORTAINER_DEPLOY_REPLICAS:-1}
      placement:
        constraints:
          - ${PORTAINER_PLACEMENT_CONSTRAINTS:-node.role==manager}
      restart_policy:
        condition: on-failure
        delay: 5s
      resources:
        limits:
          memory: ${PORTAINER_MEMORY_LIMIT:-1G}
      labels:
        - "traefik.enable=${PORTAINER_TRAEFIK_LABELS_SWARM_ENABLE:-true}"
        - "traefik.http.middlewares.${APP_NAME:-portainer}-strip.stripprefix.prefixes=${PORTAINER_BASE_PATH:-/}"
        - "traefik.http.services.${APP_NAME:-portainer}.loadbalancer.server.port=${PORTAINER_PORT:-9000}"
        - "traefik.http.routers.${APP_NAME:-portainer}.entrypoints=${PORTAINER_ENTRYPOINTS:-web}"
        - "traefik.http.routers.${APP_NAME:-portainer}.rule=Host(`${PORTAINER_DOMAIN:-portainer.example.com}`) && PathPrefix(`${PORTAINER_BASE_PATH:-/}`)"
        - "traefik.http.routers.${APP_NAME:-portainer}.service=${APP_NAME:-portainer}"
        - "traefik.http.routers.${APP_NAME:-portainer}.middlewares=${PORTAINER_MIDDLEWARES:-}"
        - "traefik.http.routers.${APP_NAME:-portainer}.tls=${PORTAINER_TLS_ENABLED:-false}"
        - "traefik.http.routers.${APP_NAME:-portainer}.tls.certresolver=${PORTAINER_TLS_CERTRESOLVER:-letsencrypt}"
        - "homepage.group=${PORTAINER_HOMEPAGE_GROUP:-Infra}"
        - "homepage.name=${PORTAINER_HOMEPAGE_NAME:-Portainer}"
        - "homepage.icon=${PORTAINER_HOMEPAGE_ICON:-portainer.png}"
        - "homepage.href=${PORTAINER_APP_URL:-http://portainer.example.com/portainer}"
        - "homepage.description=${PORTAINER_HOMEPAGE_DESCRIPTION:-Portainer CE}"
        - "homepage.siteMonitor=${PORTAINER_HOMEPAGE_SITEMONITOR:-http://server:9000}"
    labels:
      - "traefik.enable=${PORTAINER_TRAEFIK_LABELS_DOCKER_ENABLE:-true}"
      - "traefik.http.middlewares.${APP_NAME:-portainer}-strip.stripprefix.prefixes=${PORTAINER_BASE_PATH:-/}"
      - "traefik.http.services.${APP_NAME:-portainer}.loadbalancer.server.port=${PORTAINER_PORT:-9000}"
      - "traefik.http.routers.${APP_NAME:-portainer}.entrypoints=${PORTAINER_ENTRYPOINTS:-web}"
      - "traefik.http.routers.${APP_NAME:-portainer}.rule=Host(`${PORTAINER_DOMAIN:-portainer.example.com}`) && PathPrefix(`${PORTAINER_BASE_PATH:-/}`)"
      - "traefik.http.routers.${APP_NAME:-portainer}.service=${APP_NAME:-portainer}"
      - "traefik.http.routers.${APP_NAME:-portainer}.middlewares=${PORTAINER_MIDDLEWARES:-}"
      - "traefik.http.routers.${APP_NAME:-portainer}.tls=${PORTAINER_TLS_ENABLED:-false}"
      - "traefik.http.routers.${APP_NAME:-portainer}.tls.certresolver=${PORTAINER_TLS_CERTRESOLVER:-letsencrypt}"
      - "homepage.group=${PORTAINER_HOMEPAGE_GROUP:-Infra}"
      - "homepage.name=${PORTAINER_HOMEPAGE_NAME:-Portainer}"
      - "homepage.icon=${PORTAINER_HOMEPAGE_ICON:-portainer.png}"
      - "homepage.href=${PORTAINER_APP_URL:-http://portainer.example.com/portainer}"
      - "homepage.description=${PORTAINER_HOMEPAGE_DESCRIPTION:-Portainer CE}"
      - "homepage.siteMonitor=${PORTAINER_HOMEPAGE_SITEMONITOR:-http://server:9000}"
