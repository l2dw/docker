networks:
  default:
    name: ${DEFAULT_NETWORK_NAME:-dokploy-network}
    external: ${DEFAULT_NETWORK_EXTERNAL:-false}

volumes:
  woodpecker-server-data:
    name: ${WOODPECKER_SERVER_DATA_VOLUME_NAME:-woodpecker-server-data}
    external: ${WOODPECKER_SERVER_DATA_VOLUME_EXTERNAL:-false}

services:
  woodpecker-server:
    image: ${WOODPECKER_SERVER_IMAGE:-docker.io/woodpeckerci/woodpecker-server:v3}
    restart: ${WOODPECKER_RESTART:-unless-stopped}
    privileged: ${WOODPECKER_PRIVILEGED:-false}
    env_file:
      - ${WOODPECKER_SERVER_ENV_FILE:-.env.example}
    environment:
      - TZ=${TZ:-America/Toronto}
      - WOODPECKER_HOST=${WOODPECKER_HOST:-http://example.com/woodpecker}
      - WOODPECKER_OPEN=${WOODPECKER_OPEN:-true}
      - WOODPECKER_ADMIN=${WOODPECKER_ADMIN:-}
      - WOODPECKER_AGENT_SECRET=${WOODPECKER_AGENT_SECRET:-}
      - WOODPECKER_SERVER_ADDR=${WOODPECKER_SERVER_ADDR:-:8000}
      - WOODPECKER_GRPC_ADDR=${WOODPECKER_GRPC_ADDR:-:9000}
      - WOODPECKER_GITHUB=${WOODPECKER_GITHUB:-true}
      - WOODPECKER_GITHUB_CLIENT=${WOODPECKER_GITHUB_CLIENT:-}
      - WOODPECKER_GITHUB_SECRET=${WOODPECKER_GITHUB_SECRET:-}
      - WOODPECKER_GITHUB_PUBLIC_ONLY=${WOODPECKER_GITHUB_PUBLIC_ONLY:-false}
      - WOODPECKER_GITEA=${WOODPECKER_GITEA:-false}
      - WOODPECKER_GITEA_URL=${WOODPECKER_GITEA_URL:-}
      - WOODPECKER_GITEA_CLIENT=${WOODPECKER_GITEA_CLIENT:-}
      - WOODPECKER_GITEA_SECRET=${WOODPECKER_GITEA_SECRET:-}
    volumes:
      - ${WOODPECKER_SERVER_DATA_DIR:-woodpecker-server-data}:/var/lib/woodpecker
    deploy:
      mode: ${WOODPECKER_SERVER_DEPLOY_MODE:-replicated}
      replicas: ${WOODPECKER_SERVER_DEPLOY_REPLICAS:-1}
      placement:
        constraints:
          - ${WOODPECKER_SERVER_PLACEMENT_CONSTRAINTS:-node.platform.os==linux}
      restart_policy:
        condition: on-failure
        delay: 5s
      resources:
        limits:
          memory: ${WOODPECKER_SERVER_MEMORY_LIMIT:-512M}
      labels:
        - "traefik.enable=${WOODPECKER_TRAEFIK_LABELS_SWARM_ENABLE:-true}"
        - "traefik.http.services.woodpecker-server.loadbalancer.server.port=8000"
        - "traefik.http.routers.woodpecker-server.entrypoints=${WOODPECKER_ENTRYPOINTS:-web}"
        - "traefik.http.routers.woodpecker-server.rule=Host(`${WOODPECKER_DOMAIN:-example.com}`) && PathPrefix(`${WOODPECKER_BASE_PATH:-/woodpecker}`)"
        - "traefik.http.routers.woodpecker-server.service=woodpecker-server"
        - "traefik.http.routers.woodpecker-server.middlewares=${WOODPECKER_MIDDLEWARES:-}"
        - "traefik.http.routers.woodpecker-server.tls=${WOODPECKER_TLS_ENABLED:-false}"
        - "traefik.http.routers.woodpecker-server.tls.certresolver=${WOODPECKER_TLS_CERTRESOLVER:-letsencrypt}"
        - "homepage.group=${WOODPECKER_HOMEPAGE_GROUP:-CI}"
        - "homepage.name=${WOODPECKER_HOMEPAGE_NAME:-Woodpecker}"
        - "homepage.icon=${WOODPECKER_HOMEPAGE_ICON:-woodpecker.png}"
        - "homepage.href=${WOODPECKER_HOST:-http://example.com/woodpecker}"
        - "homepage.description=${WOODPECKER_HOMEPAGE_DESCRIPTION:-Woodpecker CI}"
        - "homepage.siteMonitor=${WOODPECKER_HOMEPAGE_SITEMONITOR:-http://woodpecker-server:8000}"
    labels:
      - "traefik.enable=${WOODPECKER_TRAEFIK_LABELS_DOCKER_ENABLE:-true}"
      - "traefik.http.services.woodpecker-server.loadbalancer.server.port=8000"
      - "traefik.http.routers.woodpecker-server.entrypoints=${WOODPECKER_ENTRYPOINTS:-web}"
      - "traefik.http.routers.woodpecker-server.rule=Host(`${WOODPECKER_DOMAIN:-example.com}`) && PathPrefix(`${WOODPECKER_BASE_PATH:-/woodpecker}`)"
      - "traefik.http.routers.woodpecker-server.service=woodpecker-server"
      - "traefik.http.routers.woodpecker-server.middlewares=${WOODPECKER_MIDDLEWARES:-}"
      - "traefik.http.routers.woodpecker-server.tls=${WOODPECKER_TLS_ENABLED:-false}"
      - "traefik.http.routers.woodpecker-server.tls.certresolver=${WOODPECKER_TLS_CERTRESOLVER:-letsencrypt}"
      - "homepage.group=${WOODPECKER_HOMEPAGE_GROUP:-CI}"
      - "homepage.name=${WOODPECKER_HOMEPAGE_NAME:-Woodpecker}"
      - "homepage.icon=${WOODPECKER_HOMEPAGE_ICON:-woodpecker.png}"
      - "homepage.href=${WOODPECKER_HOST:-http://example.com/woodpecker}"
      - "homepage.description=${WOODPECKER_HOMEPAGE_DESCRIPTION:-Woodpecker CI}"
      - "homepage.siteMonitor=${WOODPECKER_HOMEPAGE_SITEMONITOR:-http://woodpecker-server:8000}"
